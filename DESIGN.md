<!-- 이 파일은 복사본입니다. 정본은 daitmap/DESIGN.md 이며 거기서 고친 뒤 7개 앱에 복사합니다.
     /impeccable 명령이 실행 프로젝트 루트의 DESIGN.md 를 읽기 때문에 앱마다 사본을 둡니다. -->

# 다있맵 지도 PWA 디자인 시스템 — 공공 안내 표지 체계

이 문서가 정본이다. 지도 PWA 7종이 이 시스템을 공유하고, 각 앱 저장소에 복사본을 둔다
(`/impeccable` 명령이 실행 프로젝트 루트의 `DESIGN.md`를 읽기 때문이다).

적용 대상: `museum_map` `library_map` `hospital_map` `kindergarten_map` `voucher_map`
`summer_water` `elementary_school_map`

적용하지 않는 곳: `observatory_map`(밤하늘 다크), `dot-density-map`(무채색 에디토리얼).
둘 다 주제에서 나온 의도적 이탈이라 건드리지 않는다.

---

## 1. 방향

이 앱들은 **공공 안내 표지**다. 크림 바탕에 그라디언트 텍스트 제목을 얹은 지도앱 기본형을 거부한다.

근거는 두 가지다.

- **도로표지규칙 제8조** — 도로표지 바탕색은 녹색이 원칙이고, 도시지역 일반도로는 청색,
  문화재·관광 안내는 갈색 바탕에 흰 글자, 지방도는 황색이다.
- **상위 브랜드 계승** — 랜딩 `daitmap`이 이미 도로표지판 콘셉트로 만들어져 있고
  `Do Hyeon` / `IBM Plex Sans KR` / `JetBrains Mono` 세 활자를 쓴다. 하위 앱이 같은 활자를 쓴다.

규칙에서 나오는 것은 **어휘**이지 배분표가 아니다. 규칙 색은 4종뿐이라 7개 앱에 1:1로 배정할 수 없다.
근거가 있는 앱은 규칙대로, 나머지는 구분을 위해 확장했음을 아래 표에 명시한다.

---

## 2. 앱마다 다른 축

앱을 구분하는 것은 이 다섯 가지뿐이다. 나머지는 전부 공통이다.

1. `--sign` / `--sign-ink` / `--sign-wash` — 면색 세 줄
2. `--kind-1..N` — 지도 마커·범례 램프 (개수는 그 앱의 `KIND_ORDER` 길이와 같다)
3. 헤더 픽토그램 (`.sign-mark` 안의 SVG path)
4. 표지판 제목과 그 아래 안내 문구
5. `manifest.json` / `<meta name="theme-color">` 의 면색

### 현재 값 (규칙의 결과이지 규칙이 아니다)

| 앱 | `--sign` | 글자 | 근거 |
|---|---|---|---|
| `museum_map` | `#6E3B1F` 갈색 | 흰색 | 규칙 정합 — 문화재·관광 |
| `library_map` | `#1F5136` 진녹 | 흰색 | 규칙 정합 — 바탕 녹색 원칙 |
| `voucher_map` | `#12457E` 청색 | 흰색 | 규칙 정합 — 도시부 일반 안내 |
| `kindergarten_map` | `#F5A700` 노랑 | **검정** | 어린이보호구역 표지 |
| `elementary_school_map` | `#E2681C` 주황 | 흰색 | **확장** — 스쿨존 계열이나 유치원과 구분 필요 |
| `hospital_map` | `#A8232B` 적색 | 흰색 | **확장** — 규칙상 병원은 청색이나 voucher와 충돌, 응급의료 표식 색 차용 |
| `summer_water` | `#0E7C86` 청록 | **확장** — 물 |

`kindergarten_map`만 글자가 검정인 것은 실제 어린이보호구역 표지가 노랑 바탕 검정 글자이기 때문이다.

---

## 3. 마커 램프 유도 규칙

**색값을 복사하지 마라.** 앱마다 면색이 다르므로 규칙에서 다시 유도해야 한다.

1. 과반 범주는 **중간 명도**에 둔다. 면색 자체를 그대로 쓰면 가장 어두운 값과 붙는다.
2. 과반 범주와 최암 범주는 **명도로 확실히 갈라진다.** 수치 기준을 두지 않는다 — 면색이 밝은 앱에서는
   같은 수치가 곧바로 어긋난다.
3. 명도가 인접하는 항목끼리는 **색상을 분리**한다.
4. 면색 계열 밖으로의 색상 이탈은 **개체 수가 가장 적은 범주에만** 허용한다. 그래야 화면 대부분이
   면색 세계로 유지된다.
5. 램프 길이는 그 앱 `KIND_ORDER` 길이와 같고, 순서는 인덱스와 1:1로 대응한다.
6. **검증은 지도 전면 렌더로 한다.** 값 목록만 보고 통과시키지 않는다. 전체 마커와 범례가 한 화면에
   들어오게 찍어서, 범례 각 항목을 마커에서 실제로 집어낼 수 있는지 눈으로 확인한다.

### 면색이 밝은 앱의 처리

`kindergarten_map`은 면색이 노랑(`#F5A700`)이라 그대로 마커에 쓰면 밝은 지도 위에서 보이지 않는다.
`--kind-1`을 `#B8791A`로 어둡게 내려 잡았다. **면색과 마커 색은 같을 의무가 없다.**

### 색값은 CSS가 소유한다

`app.js`는 색을 하드코딩하지 않는다. `getComputedStyle`로 `--kind-*`를 읽어 쓴다.

```js
var ROOT_STYLE = getComputedStyle(document.documentElement);
function cssVar(name, fallback) {
  return (ROOT_STYLE.getPropertyValue(name) || '').trim() || fallback;
}
var KIND_COLOR = KIND_ORDER.reduce(function (m, k, i) {
  m[k] = cssVar('--kind-' + (i + 1), cssVar('--sign', '#333'));
  return m;
}, {});
```

---

## 4. 공통 토큰

### 중립 (7개 동일)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#F7F7F5` | 페이지 배경 |
| `--panel` | `#FFFFFF` | 카드·패널 바닥 |
| `--ink` | `#1A1A18` | 본문 |
| `--ink-muted` | `#5F5E58` | 보조 텍스트 |
| `--line` | `#DCDBD4` | 괘선·테두리 |
| `--free` | `#176B5B` | 무료 — 면 채움 |
| `--paid` | `#8A4B12` | 유료 — 외곽선 + 굵기 |
| `--tap` | `44px` | 최소 터치 타깃 |

`--free`는 면 채움, `--paid`는 외곽선이다. **유료까지 채우면 면색이 카드로 새어
"면색은 표지판·활성 필터·마커에만" 규칙이 깨진다.** 부모가 스캔하는 것은 "무료"다.

### 타이포

| 역할 | 폰트 | 용도 |
|---|---|---|
| 디스플레이 | `Do Hyeon` | 표지판 제목 |
| 본문 | `IBM Plex Sans KR` | 카드명, 필터, 안내문 |
| 수치 | `JetBrains Mono` | 시설 수, 결과 개수 |

스케일은 1.250(Major Third) **6단계 고정**이다. 이 여섯 밖의 크기를 쓰지 않는다.

```
--fs-xxl: 2.19rem   --fs-xl: 1.75rem   --fs-l: 1.40rem
--fs-m:   1.12rem   --fs-base: 1rem    --fs-s: 0.80rem
```

카드 정보(휴관일·관람시간 등 헛걸음을 막는 정보)는 `--fs-s`가 아니라 `--fs-base`를 쓴다.
야외 한 손 조작이 실제 조건이라 12.8px는 약하다.

### 면·그림자·반경

**`box-shadow`를 위계 표현에 쓰지 않는다.** 표지판은 뜨지 않는다. 위계는 면색과 1px 괘선으로만 만든다.

단 하나의 예외가 시그니처다 — 판 안쪽 흰 테두리:

```css
box-shadow: inset 0 0 0 2px rgba(255, 255, 255, .9);
```

그림자가 아니라 `inset`이다. **헤더에만 쓴다.** 여러 곳에 반복하면 시그니처가 죽는다.

반경: 패널·지도판·모달 `12px`, 카드 `10px`, pill `999px`, 입력·버튼 `8px`.

### 금지

- 그라디언트 일체 (`background-clip: text` 제목 포함)
- `:hover`의 `translateY` / `rotate` — 터치 기기에서 탭 후 고착된다
- 유니코드 글리프·이모지를 아이콘 자리에 쓰는 것
- 1px 넘는 컬러 `border-left`

---

## 5. 컴포넌트

### 헤더 — 시그니처

대담함은 여기 한 곳에만 쓴다. 면색은 **표지판·활성 필터·지도 마커**에만 등장한다.

```
모바일 (≤640px)                    데스크톱 (>640px)
┏━━━━━━━━━━━━━━━━━━━━━━━┓        ┏━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ [픽토] 앱 이름    505 ┃        ┃ [픽토]  앱 이름       505 ┃
┗━━━━━━━━━━━━━━━━━━━━━━━┛        ┗━━━━━━━━━━━━━━━━━━━━━━━━━┛
 참고용입니다. …                    참고용입니다. …
```

- 판 최소 높이 52px, 면색 바탕 + `inset` 흰 테두리 2px
- 좌측 픽토그램 1개, 중앙 제목(`Do Hyeon`), 우측 시설 수(`JetBrains Mono`)
- **제목을 줄이지 않는다.** 좁으면 `word-break: keep-all`로 어절 단위 두 줄이 된다.
  `text-overflow: ellipsis`를 쓰지 않는다 — 표지판이 이름을 못 실으면 표지판이 아니다
- 안내 문구는 판 **밖** 회색 캡션이다. 판 안에 넣으면 초점을 뺏는다
- 링크 복사 버튼은 판 안쪽 우측, 44×44px

### 아이콘 — 두 계열

| 계열 | 클래스 | 방식 | 용도 |
|---|---|---|---|
| 표지판 픽토그램 | `.sign-mark` | `fill: currentColor` 면 채움 | 헤더 1개 |
| 컨트롤 아이콘 | `.ico` | `stroke-width: 1.8` 선 | 찜·필터·닫기·공유 |

둘 다 직접 그린 SVG다. 유니코드 글리프나 이모지를 쓰지 않는다.

`.ico`에 `pointer-events: none`을 준다. 클릭 판정이 항상 버튼에 잡히게 하기 위해서다.

### 카드

**카드를 면색으로 채우지 않는다.** 수십~수천 장이 진한 면색으로 깔리면 스캔이 불가능해지고,
헤더 시그니처가 죽는다. 모바일 1열에서는 더 답답해진다.

흰 판 + 1px 괘선 + `10px` 반경. hover는 포인터 기기에서만, 테두리 색과 `--sign-wash` 바닥으로.

### 필터

- `.pill` — 알약 형태 유지, 그림자 없음, 활성은 면색 단색 + `--sign-ink`
- 입력·select는 알약이 아니라 `8px` 반경. 입력 필드가 알약이면 표지 체계에서 뜬다
- 뷰 전환은 **세그먼트 컨트롤 하나로 묶고**, 찜 필터는 그 밖의 독립 pill로 뗀다.
  모드 스위치와 필터가 같은 형태로 붙어 있으면 구분되지 않는다

### 모달

`≤640px`에서 하단 고정 시트, 그 위로는 가운데 카드. 시트 상단 그랩 핸들은
`position: static`이어야 한다 — `sticky`면 스크롤 시 본문이 핸들 뒤로 통과한다.

---

## 6. 모바일 우선

이 앱들의 주 사용처는 외출 중 휴대폰이다. 모바일을 기준선으로 두고 위로 올린다.
브레이크포인트는 `640px` / `900px` 두 단계.

- `<meta name="viewport" ... viewport-fit=cover>` + `env(safe-area-inset-*)`.
  홈 화면에 추가해 전체화면으로 쓰는 PWA라 없으면 노치에 제목이 가린다
- 모든 인터랙티브 요소 `min-height: 44px`. 시각적으로 작아야 하면 음수 마진으로 히트 영역만 확보한다
- 지도 높이는 `vh`가 아니라 `dvh`. 주소창이 접힐 때 튄다
- **입력 `font-size`를 16px 미만으로 내리지 않는다.** iOS Safari가 포커스 시 페이지를 확대한다
- `<select>`에 `min-width: 0`을 준다. 없으면 가장 긴 옵션 텍스트만큼 벌어져 옆 버튼을 다음 줄로 밀어낸다

---

## 7. 함정 (실제로 겪은 것)

### `prefers-reduced-motion` — 상태가 사라진다

흔히 쓰는 스니펫은 이렇게 되어 있다.

```css
/* 이렇게 쓰면 안 된다 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { transition-duration: .01ms !important; }
}
```

`transition-property`가 기본값 `all`인 채로 남아, **모션 감소를 켠 사용자에게는 배경·테두리·transform
같은 상태 변화가 시작값에 고정돼 아예 렌더되지 않는다.** 활성 필터도, 필터 열림 상태도 보이지 않는다.
전환 자체를 꺼야 한다.

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 1ms !important;
    animation-iteration-count: 1 !important;
    transition: none !important;
    scroll-behavior: auto !important;
  }
}
```

### 서비스워커 캐시 — 안 바뀐 것처럼 보인다

자산을 바꾸면 `sw.js`의 캐시 버전을 **반드시** 올린다. 안 올리면 기존 사용자에게 옛 파일이 계속 나간다.
개발 중 검증할 때도 등록 해제 + 캐시 삭제 + 리로드를 하지 않으면 옛 화면을 보고 판단하게 된다.

### JS가 아이콘을 글리프로 덮어쓰는 코드

상태 표시를 `el.textContent = '▲'` 같은 글리프 교체로 하던 코드가 있으면, 넣어둔 SVG를 지운다.
`[aria-expanded="true"]` 같은 어트리뷰트 선택자 + CSS로 바꾼다.

### 리포트 페이지가 같은 스타일시트를 쓴다

`report.html`이 `index.html`과 `style.css`를 공유한다. 토큰만 갈아끼우면 리포트가 깨진다.
같은 작업 단위에서 처리한다.

---

## 8. 품질 바닥

- `:focus-visible` — 전 인터랙티브 요소에 `outline: 2px solid var(--sign); outline-offset: 2px`
- `prefers-reduced-motion` — 위 형태로
- 브라우저 표면도 테마한다: `::selection`, 스크롤바, `caret-color`, `accent-color`,
  수치에 `font-variant-numeric: tabular-nums`
- **독도를 항상 직접 표시한다.** 배경 지도 표기에 기대지 않는다.
  위도 37.2429 / 경도 131.8664, 경상북도 울릉군 울릉읍 독도리
- 지도 UI에 국기 이모지를 쓰지 않는다

---

## 9. 다음 단계 (아직 안 한 것)

- **종류별 픽토그램** — 표지 체계의 본래 장치는 색이 아니라 픽토그램이다. 마커를 종류별 픽토그램으로
  바꾸면 `--kind-*` 램프가 통째로 필요 없어진다. Leaflet 캔버스 렌더에 수천 점이라 성능 검토가 먼저다
- **`inset` 흰 키라인 확장** — 지금은 헤더 1회. 활성 필터·팝업·범례가 같은 키라인을 반복하면
  표지 세계가 페이지 전체로 넓어진다. 다만 시그니처가 묽어지지 않는 선을 찾아야 한다
- **방향·거리 표기** — 표지판의 핵심 어휘인 화살표와 거리 수치. geolocation 기능이 없어 보류 중이다
- **범례를 판으로** — 지금은 떠 있는 흰 상자다

---

## 10. 배포 체크리스트

앱 하나를 고칠 때마다 확인한다.

- [ ] `sw.js` 캐시 버전을 올렸다
- [ ] `manifest.json`의 `theme_color` / `background_color`, `<meta name="theme-color">`가 면색과 맞는다
- [ ] `report.html`이 정상 렌더된다
- [ ] 지도 전면 렌더로 마커·범례 대응을 눈으로 확인했다
- [ ] 390px에서 표지판 제목이 잘리지 않는다
- [ ] 모션 감소를 켠 상태에서 활성 필터·열림 상태가 보인다
- [ ] 독도가 표시된다
- [ ] 44px 미만 터치 타깃이 없다
- [ ] 검색·필터·찜·모달·팝업이 동작한다
